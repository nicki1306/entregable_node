<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CODERStore - Registro</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-900 text-gray-100">

    <div class="max-w-md mx-auto mt-16 p-8 rounded-lg bg-gray-800 border border-gray-700">
        <h1 class="text-3xl font-bold text-center mb-6">CODERStore</h1>
        <h2 class="text-2xl font-bold text-center mb-8">Registro</h2>

        <form action="/api/auth/register" method="post">
            <div class="mb-4">
                <label class="block mb-2" for="firstName">Nombre</label>
                <input type="text" name="firstName" id="firstName" class="w-full p-2 border border-gray-600 rounded bg-gray-700 focus:outline-none focus:border-gray-500" placeholder="Nombre" required autocomplete="nombre" autofocus>
            </div>
            <div class="mb-4">
                <label class="block mb-2" for="lastName">Apellido</label>
                <input type="text" name="lastName" id="lastName" class="w-full p-2 border border-gray-600 rounded bg-gray-700 focus:outline-none focus:border-gray-500" placeholder="Apellido" required autocomplete="apellido">
            </div>
            <div class="mb-4">
                <label class="block mb-2" for="email">Email</label>
                <input type="email" name="email" id="email" class="w-full p-2 border border-gray-600 rounded bg-gray-700 focus:outline-none focus:border-gray-500" placeholder="Email" required autocomplete="email">
            </div>
            <div class="mb-4">
                <label class="block mb-2" for="password">Contraseña</label>
                <input type="password" name="password" id="password" class="w-full p-2 border border-gray-600 rounded bg-gray-700 focus:outline-none focus:border-gray-500" required autocomplete="password">
            </div>
            <button type="submit" class="w-full bg-green-500 text-gray-100 p-2 rounded hover:bg-green-600 focus:outline-none">Registrarse</button>

            {{#if msg}}
            <div class="mt-6">
                <div class="bg-red-500 text-gray-100 p-2 rounded">{{msg}}</div>
            </div>
            {{/if}}

            <div class="mt-6 text-center">
                Ya tienes cuenta? <a href="/login" class="text-green-400 hover:text-green-300">Ingresa</a>
            </div>
        </form>
    </div>

</body>
</html>
